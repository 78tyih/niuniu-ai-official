# 产品事实核查清单 · Learning Center V1

日期：2026-09-10
用途：列出学习中心内容中**无法从现有资料确认**的产品事实，供产品负责人逐条确认。
原则：**未确认的内容不写成断言**，网页文案统一采用「请以产品当前版本说明为准」表述。

---

## 1. 高优先级（已影响用户理解，建议优先确认）

| # | 主题 | 现状表述 | 需要确认 |
|---|---|---|---|
| 1 | 每日最大亏损 · 触发范围 | 「达到每日最大亏损后，当天会停止开新仓」 | 触发后是否影响**已有持仓 / 挂单 / 手工操作 / 其他执行通道**？现有资料不足以判断，网页未做任何推断。 |
| 2 | 每日额度 · 重置口径 | 「每日额度什么时候重置」列为待确认 | 统计起止时间、是否跨交易日、按服务器时间还是本地时间 |
| 3 | 每日最大亏损 · 单位与计算口径 | 未写具体单位 | 是金额还是百分比？按已实现还是含未实现？ |
| 4 | 持仓监控 · 自动处理 | 「持仓出现风险时会自动处理」 | 自动处理的**具体动作**是什么（减仓 / 平仓 / 提示）？触发条件是什么？ |
| 5 | 执行度 · 判定含义 | 网页已规避「把握」表述，写为「是否提交开仓的筛选门槛」 | 「最低开仓执行度」的**准确定义**（现有资料不足以给出口径） |

---

## 2. 视频内已烧录、需重渲染的表述

| 视频 | 字幕原文 | 问题 | 处理 |
|---|---|---|---|
| `11-ai-model-execution` | 「它决定 AI 有多大把握才会开仓。」 | 「把握」属需规避的表述，容易被理解为胜率/盈利概率 | **视频内已烧录，本次未修改**。网页文案已规避。详见 `VIDEO_TITLE_RERENDER_REQUIRED.md` |

---

## 3. 数值类（需产品确认默认值）

| # | 位置 | 现状 | 需确认 |
|---|---|---|---|
| 6 | 风控 · 每日最大交易次数 | 视频字幕与网页均写「默认 20 次」 | 默认值是否确为 20？是否会随版本变化？ |
| 7 | 持仓监控 · 分析间隔 | 视频字幕与网页均写「默认 5 分钟」 | 默认值是否确为 5 分钟？ |
| 8 | 自动盯盘 · 分析间隔 | 网页写「默认每隔几分钟」 | 自动交易的默认分析间隔 |
| 9 | 参数 · 每单手数 | 视频字幕写「默认是 0.01 手」 | 默认手数是否确为 0.01？是否因品种不同？ |
| 10 | 截图分析 · 响应时间 | 网页写「AI 大约需要半分钟给出结果」 | 是否与实际相符？是否需要加「视情况而定」 |
| 11 | 参数 · 止损止盈点数 | 网页写「具体含义与生效方式请以产品当前版本为准」 | 点数的换算方式与生效范围 |

---

## 4. 按 SPEC §26 清单逐项核对

| 检查项 | 结论 |
|---|---|
| MT5 平台支持 | 现有资料只写「原则上 MT5 环境均可连接，个别券商接口可能存在例外」。**未做「支持所有券商」类表述**，保持原有免责口径。 |
| 自动执行行为 | 网页统一表述为「AI 给方案、用户确认后执行」。**是否存在全自动执行通道、是否可配置，现有资料不足以确认**，未写成断言。 |
| AI-2 行为 | 现有资料未提供 AI-2 / AI-3 的独立定义，学习中心**未使用这些术语**，避免误述。 |
| AI-3 行为 | 同上。 |
| 牛气值扣费 | 采用「每次自动分析记录一次扣费」+「具体额度与折算规则以购买页与账户页面显示为准」。**单次扣减点数未做断言**。 |
| 交易阈值 | 见 §1 第 5 项，未给出口径。 |
| 历史订单 | 采用「按时间顺序查看订单记录」，**未描述具体的下单/成交字段结构**。 |
| Prompt | 采用「分析、风控、监控三段提示词」，来自视频字幕；**是否还有更多段落未确认**。 |
| 克隆分析师 | 采用「选周期 → 选类型（激进/保守）→ 补充要求 → 保存到提示词」，来自视频字幕。**生成算法与可选类型全集未确认**。 |
| 数据存储 | 现有 `faq.ts` 中有「数据安全」条目。学习中心**未新增任何数据存储类描述**，建议由产品补充正式口径后再写入。 |

---

## 5. 原有合规表述（已保留，无需修改）

以下口径来自现有内容，本次沿用：

- 牛牛 AI **不承诺任何收益**，AI 输出仅供辅助参考
- **不提供胜率、收益率等数据**
- 所有交易决策由用户自己做出，风险由用户自己承担
- FAQ 中「AI 分析准确率」条目已说明质量受多种因素影响

---

## 6. 处理建议

1. **§1 的 5 项**建议产品负责人逐条给出书面口径，我据此更新对应文章的 `steps` / `commonProblems`
2. **§3 的数值**建议核对一次，若与实际不符，只需改 `src/content/learn/articles.ts` 单处（唯一数据源）
3. **§2 的视频表述**随视频重渲染一并处理
